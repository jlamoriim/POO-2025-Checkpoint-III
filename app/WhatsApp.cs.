public classe WhatsApp
{
    public void EnviarMnesagem(string numero, TextMassage message)
    {
         Console.WriteLine($"Enviando mensagem de texto para o número {numero}")
    }
}

  public void EnviarMnesagem(string numero, VideoMassage message)
    {
         Console.WriteLine($"Enviando mensagem de texto para o número {numero}")

    }
      public void EnviarMnesagem(string numero, PhotoMassage message)
    {
         Console.WriteLine($"Enviando mensagem de texto para o número {numero}")

    }
      public void EnviarMnesagem(string numero, FileMassage message)
    {
         Console.WriteLine($"Enviando mensagem de texto para o número {numero}")

    }